defmodule RemoveDuplicatedFromSortedArrayTest do
  use ExUnit.Case
  doctest RemoveDuplicatedFromSortedArray

  test "greets the world" do
    assert RemoveDuplicatedFromSortedArray.hello() == :world
  end
end
